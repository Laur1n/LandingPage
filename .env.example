# Copy to .env only if you add local tooling that reads env vars.
# Build and deploy need no secrets — GitHub Actions uses built-in credentials.
# CMS git-gateway auth is configured in public/admin/config.yml (see docs/cms-auth-setup.md).
